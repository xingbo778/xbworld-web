HTML5 2D Canvas Renderer for XBWorld
=====================================

The default 2D HTML5 canvas renderer for the XBWorld web client.

Renders the game map, units, cities, and terrain using the HTML5 Canvas API.
